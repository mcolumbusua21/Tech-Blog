{{!-- <ul class="post-column">
  {{#each posts as |post|}}
  <li>{{>post-info post}}</li>
  {{/each}}
</ul> --}}

<div>
  {{#each clearData}}
  <div class="post-card">
    <h2>{{title}}</h2>
    <h3>by {{User.username}}</h3>
    <p class="post-content">
      {{body}}
    </p>
  </div>
  {{/each}}
</div>